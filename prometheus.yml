global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'mlflow_exporter'
    scrape_timeout: 4s        # Batas toleransi waktu penarikan data dinaikkan
    static_configs:
      - targets: ['192.168.1.20:8000']